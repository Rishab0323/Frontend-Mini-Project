const toggles = documents.querySelectorAll('faq-toggle')

toggles.forEach(toggles => {
    toggles.addEventListener('click',() => {
        toggles.parentNpde.clasList.tooggle
        ('active')
    })
})